import React, { useState } from "react";
import NavA from "./NavA";

const Admin = (props) => {

    return(
        <div>
            <NavA />
            <h1>Admin Page</h1>
        </div>
    )
}

export default Admin;